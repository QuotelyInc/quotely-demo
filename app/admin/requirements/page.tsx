import RequirementDashboard from '../../../components/RequirementDashboard';

export default function RequirementsPage() {
  return <RequirementDashboard />;
}

export const metadata = {
  title: 'Requirements Tracker | Quotely Admin',
  description: 'Track and manage all Quotely platform requirements',
};